# Reviewer Checklist

Before submission confirm:

- original three-signal mask choreography is preserved;
- signal A, B and C paths are unchanged;
- 1.44s loop is unchanged;
- core pulse still occurs after the third signal;
- the mark never rotates;
- the mark never breaks apart;
- exact source SVG hash still matches;
- root `tri-signal.svg` equals `spinner-kit/tri-signal.svg`;
- light demo works;
- dark demo works;
- 16, 20, 24, 32, 48 and 64px examples exist;
- loading.html uses the exact root SVG;
- repository is public;
- GitHub Pages is live;
- live SVG matches repository SVG;
- README contains no placeholders;
- final URLs are real;
- working tree is clean.
